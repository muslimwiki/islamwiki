<!-- Muslim Skin Header Component -->
<header class="muslim-header">
    <!-- Top Bar -->
    <div class="muslim-header-top">
        <div class="muslim-header-container">
            <div class="muslim-header-left">
                <a href="{{ logo.url }}" class="muslim-logo">
                    <span class="logo-icon">{{ logo.icon }}</span>
                    <span class="logo-text">{{ logo.text }}</span>
                </a>
            </div>
            
            <div class="muslim-header-center">
                {% if search.enabled %}
                    <div class="muslim-search">
                        <form class="search-form" method="GET" action="{{ search.action }}">
                            <div class="search-input-wrapper">
                                <input type="text" name="q" class="search-input" 
                                       placeholder="{{ search.placeholder }}" 
                                       value="{{ app.request.query.get('q', '') }}">
                                <button type="submit" class="search-button">
                                    <svg class="search-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                                    </svg>
                                </button>
                            </div>
                        </form>
                    </div>
                {% endif %}
            </div>
            
            <div class="muslim-header-right">
                <nav class="muslim-nav">
                    {% for item in navigation %}
                        <a href="{{ item.url }}" class="nav-link {{ is_current_page(item.url) ? 'active' : '' }}">
                            {{ item.icon }} {{ item.label }}
                        </a>
                    {% endfor %}
                </nav>
                
                <div class="muslim-user-menu">
                    {% if user %}
                        <div class="user-dropdown" z-data='{"open": false}'>
                            <button z-click="open = !open" class="user-button" aria-label="User menu for {{ user.username }}">
                                <div class="user-avatar">
                                    <svg class="avatar-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                                    </svg>
                                </div>
                                <span class="user-name">{{ user.username }}</span>
                                <svg class="dropdown-arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                                </svg>
                            </button>
                            
                            <div z-show="open" 
                                 z-click-away="open = false"
                                 class="user-dropdown-menu">
                                {% for item in user_menu_items %}
                                    <a href="{{ item.url }}" class="dropdown-item {{ is_current_page(item.url) ? 'active' : '' }}">
                                        <svg class="dropdown-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"/>
                                        </svg>
                                        {{ item.label }}
                                    </a>
                                {% endfor %}
                                <div class="dropdown-divider"></div>
                                <form action="/logout" method="POST" class="dropdown-form">
                                    <button type="submit" class="dropdown-item logout-item">
                                        <svg class="dropdown-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                                        </svg>
                                        Logout
                                    </button>
                                </form>
                            </div>
                        </div>
                    {% else %}
                        <div class="auth-buttons">
                            <a href="/login" class="btn btn-outline">Sign In</a>
                            <a href="/register" class="btn btn-primary">Join</a>
                        </div>
                    {% endif %}
                </div>
                
                <!-- Mobile menu toggle -->
                <button class="mobile-menu-toggle" aria-label="Toggle mobile menu">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
            </div>
        </div>
    </div>
    
    <!-- Secondary Navigation -->
    <div class="muslim-header-secondary">
        <div class="muslim-header-container">
            <nav class="muslim-secondary-nav">
                {% for item in secondary_navigation %}
                    <a href="{{ item.url }}" class="nav-link {{ is_current_page(item.url) ? 'active' : '' }}">
                        {{ item.icon }} {{ item.label }}
                    </a>
                {% endfor %}
            </nav>
        </div>
    </div>
</header> 